# Contributing

This wiki is maintained by the ProjectAmazonPH training team.

For new pages, use beginner-friendly language. Every concept should be explained as if teaching someone who has never seen the Amazon Ads Console before.

Before submitting changes:

```bash
pip install -r requirements.txt
mkdocs build --strict
```

Make sure the build passes with no warnings.
